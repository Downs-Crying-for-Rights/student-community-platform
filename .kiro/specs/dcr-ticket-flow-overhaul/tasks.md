# 实施计划

- [x] 1. 编写 Bug 条件探索性测试
  - **Property 1: Fault Condition** - 工单列表查询冲突与多人互助限制
  - **重要**: 此属性测试必须在实施修复之前编写和运行
  - **目标**: 通过反例证明 Bug 存在，确认根因分析
  - **Scoped PBT 方法**: 针对以下确定性 Bug 场景编写属性测试：
  - 场景 A（查询冲突）: 对于任意拥有 dcrAccess 的非 Admin 用户，当使用 `status=IN_PROGRESS` 筛选时，GET /api/cases 的 `where = { status: "IN_PROGRESS", OR: [{ handlerId: userId }, { status: "OPENED" }, { submitterId: userId }] }` 导致用户自己提交但由他人处理的工单不在结果中（因为 Prisma 将顶层 status 与 OR 视为 AND 关系，`{ status: "OPENED" }` 子句与外层 `status: "IN_PROGRESS"` 矛盾）
  - 场景 B（OPENED 消息限制）: `canSendMessage("OPENED")` 返回 false，提交者无法在 OPENED 状态补充信息
  - 场景 C（多人互助）: Helper B 尝试加入已有 Helper A 处理的 IN_PROGRESS 工单，PATCH API 要求当前状态为 OPENED 才能接单，无法在 IN_PROGRESS 状态加入
  - 场景 D（帖子可见性）: 用户 A 参与工单 #1，但能在 `/dcr/posts` 看到工单 #2 的帖子（因 Post 无 caseId 字段，查询仅按 `zone=DCR`）
  - 测试文件: `src/app/api/cases/__tests__/ticket-flow-overhaul.fault.property.test.ts`
  - 在未修复代码上运行测试
  - **预期结果**: 测试失败（这是正确的——证明 Bug 存在）
  - 记录发现的反例以理解根因
  - 当测试编写完成、运行并记录失败后标记任务完成
  - _Requirements: 1.1, 1.6, 1.8, 1.9, 1.11_

- [x] 2. 编写保持性属性测试（在实施修复之前）
  - **Property 2: Preservation** - Admin 查询、状态机规则与消息约束不变
  - **重要**: 遵循观察优先方法论
  - 观察: 在未修复代码上运行以下场景并记录实际输出：
  - 观察 1: Admin 用户调用 GET /api/cases 返回所有工单（不受 OR 条件限制）
  - 观察 2: 状态机转换 OPENED→IN_PROGRESS→NEED_MORE_INFO→IN_PROGRESS 和 OPENED→CLOSED、IN_PROGRESS→CLOSED 均正常工作
  - 观察 3: CLOSED 状态下 `canSendMessage("CLOSED")` 返回 false
  - 观察 4: 消息发送时 `isAnonymous` 始终为 true
  - 观察 5: DCR_HELPER 并发处理上限 5 个活跃工单
  - 观察 6: `computeFlowStep` 函数输出不变
  - 观察 7: 没有 dcrAccess 的用户仅能查看自己提交的工单
  - 编写属性测试: 对于所有非 Bug 条件输入，验证上述行为在修复前后保持一致
  - 测试文件: `src/app/api/cases/__tests__/ticket-flow-overhaul.preservation.property.test.ts`
  - 在未修复代码上运行测试
  - **预期结果**: 测试通过（确认基线行为已捕获）
  - 当测试编写完成、运行并在未修复代码上通过后标记任务完成
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.7, 3.8, 3.9_

- [x] 3. 数据模型变更（Prisma Schema）
  - [x] 3.1 新增 CaseHandler 关联表
    - 在 `prisma/schema.prisma` 中新增 `CaseHandler` 模型，包含 `id`、`caseId`、`userId`、`joinedAt` 字段
    - 添加 `@@unique([caseId, userId])` 唯一约束
    - 添加 `case_` 和 `user` 关联，`onDelete: Cascade`
    - 在 `Case` 模型中新增 `handlers CaseHandler[]` 关联
    - 在 `User` 模型中新增 `caseHandlers CaseHandler[]` 关联
    - _Bug_Condition: isBugCondition(input) where action="JOIN_CASE" AND caseHasHandler=true_
    - _Expected_Behavior: 支持多个处理者通过 CaseHandler 关联表加入同一工单_
    - _Preservation: Case.handlerId 保留作为主处理者向后兼容_
    - _Requirements: 2.11, 2.12_
  - [x] 3.2 Post 模型新增 caseId 字段
    - 在 `Post` 模型中新增 `caseId String?` 字段和 `case_ Case? @relation(fields: [caseId], references: [id])` 关联
    - 在 `Case` 模型中新增 `posts Post[]` 关联
    - _Bug_Condition: isBugCondition(input) where action="VIEW_DCR_POSTS" AND isParticipantOfAllVisiblePosts=false_
    - _Expected_Behavior: DCR 帖子通过 caseId 关联到工单，可见性由工单参与者决定_
    - _Requirements: 2.9, 2.10_
  - [x] 3.3 生成并应用数据库迁移
    - 运行 `npx prisma migrate dev --name add-case-handler-and-post-caseid`
    - 验证迁移文件正确生成
    - _Requirements: 2.11, 2.10_

- [x] 4. 修复工单列表查询逻辑
  - [x] 4.1 重构 GET /api/cases 的 where 条件构建
    - 修改 `src/app/api/cases/route.ts` 中 GET handler 的查询逻辑
    - 将 status 筛选从顶层字段移入 AND/OR 组合结构
    - 对于 DCR_HELPER/dcrAccess 用户: 构建 `where.AND = [{ OR: [{ handlers: { some: { userId } } }, { submitterId: userId }, ...(!status || status === "OPENED" ? [{ status: "OPENED" }] : [])] }, ...(status ? [{ status }] : [])]`
    - 确保 Admin/SUPER_ADMIN 查询逻辑不变（仅在有 status 时添加 `where.status`）
    - 确保无 dcrAccess 用户仅查看自己提交的工单
    - 更新 include 中的 `handler` 为同时包含 `handlers` 关联
    - _Bug_Condition: isBugCondition(input) where action="LIST_CASES" AND statusFilter!=null AND userRole!="ADMIN"_
    - _Expected_Behavior: status 筛选与 OR 条件正确组合，用户始终能看到自己提交和处理的工单_
    - _Preservation: Admin 全量查询不变（3.1），无 dcrAccess 用户仅看自己工单（3.2）_
    - _Requirements: 2.1, 2.2, 3.1, 3.2_

- [x] 5. 实现多人互助接单逻辑
  - [x] 5.1 扩展 PATCH /api/cases/[id] 支持多人加入
    - 修改 `src/app/api/cases/[id]/route.ts` 中 PATCH handler
    - 新增 "JOIN" action: 允许 DCR_HELPER 在 OPENED 或 IN_PROGRESS 状态下加入工单
    - 接单时（OPENED→IN_PROGRESS）: 同时创建 CaseHandler 记录并设置 handlerId（主处理者）
    - 加入时（IN_PROGRESS + JOIN）: 仅创建 CaseHandler 记录，不修改 handlerId
    - 检查工单处理者数量 < 5（通过 CaseHandler count）
    - 检查用户并发活跃工单数 < 5（通过 CaseHandler + Case status 联合查询）
    - 加入时生成 TimelineEvent 记录和通知
    - _Bug_Condition: isBugCondition(input) where action="JOIN_CASE" AND caseHasHandler=true AND handlerCount<5_
    - _Expected_Behavior: 多个 Helper 可加入同一工单，上限 5 人_
    - _Preservation: 状态机规则不变（3.4），并发上限 5 个（3.7）_
    - _Requirements: 2.11, 2.12, 3.4, 3.7_
  - [x] 5.2 更新 GET /api/cases/[id] 权限检查
    - 修改 `src/app/api/cases/[id]/route.ts` 中 GET handler
    - 将 `isHandler = caseRecord.handlerId === userId` 扩展为检查 CaseHandler 关联表
    - include 中新增 `handlers: { select: { userId: true, user: { select: { id: true, nickname: true } } } }`
    - _Requirements: 3.6_

- [x] 6. 消息系统改造
  - [x] 6.1 扩展 canSendMessage 函数
    - 修改 `src/lib/dcr-ui-helpers.ts` 中 `canSendMessage` 函数
    - 新增 `isSubmitter` 可选参数: `canSendMessage(status: CaseStatus, isSubmitter?: boolean): boolean`
    - OPENED 状态下: 当 `isSubmitter=true` 时返回 true（允许提交者补充信息）
    - IN_PROGRESS/NEED_MORE_INFO: 保持返回 true
    - CLOSED: 保持返回 false
    - _Bug_Condition: isBugCondition(input) where action="SEND_MESSAGE" AND caseStatus="OPENED" AND isSubmitter=true_
    - _Expected_Behavior: OPENED 状态提交者可单向发送补充信息_
    - _Preservation: CLOSED 状态禁止发消息（3.5），isAnonymous=true（3.8）_
    - _Requirements: 2.8, 3.5, 3.8_
  - [x] 6.2 改造消息 API 支持群组通道和 OPENED 状态
    - 修改 `src/app/api/cases/[id]/messages/route.ts`
    - GET: 权限检查扩展为 CaseHandler 关联表（`isHandler = handlers.some(h => h.userId === userId)`）
    - POST: 状态放宽——OPENED 状态下允许提交者发送（单向补充信息）
    - POST: 移除 receiverId 单一接收者逻辑，消息通过 caseId 关联，所有参与者可见（群组模式）
    - 保持 `isAnonymous: true`
    - _Bug_Condition: isBugCondition(input) where action="SEND_MESSAGE" AND participantCount>2_
    - _Expected_Behavior: 所有工单参与者在同一消息面板中查看和发送消息_
    - _Preservation: isAnonymous=true（3.8），CLOSED 禁止发消息（3.5）_
    - _Requirements: 2.6, 2.8, 3.5, 3.8_
  - [x] 6.3 MessagePanel 新增轮询和群组支持
    - 修改 `src/components/dcr/MessagePanel.tsx`
    - 新增 15 秒轮询机制自动刷新消息列表
    - 使用 `document.visibilitychange` 事件，页面重新可见时立即刷新
    - 传递 `isSubmitter` prop 用于 OPENED 状态发送权限判断
    - 组件卸载时清理轮询定时器
    - _Bug_Condition: isBugCondition(input) where action="WAIT_MESSAGE" AND hasPolling=false_
    - _Expected_Behavior: 每 15 秒自动刷新消息，页面可见性变化时触发刷新_
    - _Requirements: 2.7_

- [x] 7. 帖子可见性控制
  - [x] 7.1 修改 DCR 帖子页面查询逻辑
    - 修改 `src/app/dcr/posts/page.tsx`
    - 替换 `zone=DCR` 查询为: 先获取用户参与的工单 ID 列表（通过 CaseHandler + submitterId），再查询 `Post.caseId IN caseIds`
    - 仅显示与用户参与工单关联的帖子
    - _Bug_Condition: isBugCondition(input) where action="VIEW_DCR_POSTS" AND isParticipantOfAllVisiblePosts=false_
    - _Expected_Behavior: 仅显示用户作为提交者或处理者参与的工单关联帖子_
    - _Requirements: 2.9, 2.10_

- [x] 8. UI 流程引导和侧边栏导航
  - [x] 8.1 工单列表页新增流程引导
    - 修改 `src/app/dcr/tickets/page.tsx`
    - 在工单列表顶部新增流程引导组件，展示工单状态流转说明：待处理(OPENED)→处理中(IN_PROGRESS)→待补充(NEED_MORE_INFO)→已关闭(CLOSED)
    - 添加说明文字区分"准入审核"（四步流程）和"工单互助流程"（新建→等待接单→处理→关闭）
    - _Bug_Condition: isBugCondition(input) where action="VIEW_TICKET_LIST" AND isFirstVisit=true_
    - _Expected_Behavior: 用户能清晰理解工单状态流转和操作指引_
    - _Requirements: 2.3, 2.4_
  - [x] 8.2 侧边栏新增 DCR 子导航
    - 修改 `src/components/layout/Sidebar.tsx`
    - 扩展 `zoneNavItems` 数组，新增：
      - `{ href: "/dcr/helper", label: "Helper 工作台", requireDcrAccess: true, minRole: "DCR_HELPER" }` — 仅 DCR_HELPER/ADMIN 可见
      - `{ href: "/dcr/posts", label: "DCR 帖子", requireDcrAccess: true }` — 所有 dcrAccess 用户可见
    - _Bug_Condition: isBugCondition(input) where action="NAVIGATE_SIDEBAR" AND targetPage IN ["helper","posts"]_
    - _Expected_Behavior: 侧边栏提供工单列表、Helper 工作台、DCR 帖子的独立入口_
    - _Preservation: 现有导航项不变（3.9）_
    - _Requirements: 2.5_

- [x] 9. 验证修复
  - [x] 9.1 验证 Bug 条件探索性测试现在通过
    - **Property 1: Expected Behavior** - 工单列表查询正确与多人互助支持
    - **重要**: 重新运行任务 1 中的同一测试——不要编写新测试
    - 任务 1 中的测试编码了期望行为
    - 当此测试通过时，确认期望行为已满足
    - 运行 `src/app/api/cases/__tests__/ticket-flow-overhaul.fault.property.test.ts`
    - **预期结果**: 测试通过（确认 Bug 已修复）
    - _Requirements: 2.1, 2.6, 2.8, 2.9, 2.11_
  - [x] 9.2 验证保持性属性测试仍然通过
    - **Property 2: Preservation** - Admin 查询、状态机规则与消息约束不变
    - **重要**: 重新运行任务 2 中的同一测试——不要编写新测试
    - 运行 `src/app/api/cases/__tests__/ticket-flow-overhaul.preservation.property.test.ts`
    - **预期结果**: 测试通过（确认无回归）
    - 确认所有测试在修复后仍然通过（无回归）
    - _Requirements: 3.1, 3.2, 3.4, 3.5, 3.7, 3.8, 3.9_

- [x] 10. 检查点 - 确保所有测试通过
  - 运行完整测试套件，确保所有测试通过
  - 如有问题请询问用户
